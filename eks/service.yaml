apiVersion: v1
kind: Service
metadata:
  name: serving-api-service
spec:
  selector:
    app: serving-api
  ports:
    - name: service
      protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
